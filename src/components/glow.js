import { settings } from "../state/settings.js"; let el = null; let isPlayerActive = false; export function mountGlowBorder() { el = document.createElement("div"); el.className = "glow-border"; document.body.appendChild(el); apply(); settings.onChange(apply); return el; } export function setPlayerActive(active) { isPlayerActive = active; apply(); } function apply() { if (!el) return; const s = settings.get().glow; const shouldHide = isPlayerActive && s.hideDuringGames; const active = s.enabled && !shouldHide; el.classList.toggle("active", active); el.classList.remove("rainbow", "duo"); el.style.removeProperty("--glow-c1"); el.style.removeProperty("--glow-c2"); if (!active) return; if (s.preset === "rainbow") { el.classList.add("rainbow"); } else if (s.preset === "green-blue") { el.classList.add("duo"); el.style.setProperty("--glow-c1", "#00e676"); el.style.setProperty("--glow-c2", "#00c8ff"); } else if (s.preset === "blue-purple") { el.classList.add("duo"); el.style.setProperty("--glow-c1", "#2979ff"); el.style.setProperty("--glow-c2", "#7c4dff"); } else if (s.preset === "custom") { el.classList.add("duo"); el.style.setProperty("--glow-c1", s.customColor1); el.style.setProperty("--glow-c2", s.customColor2); } el.style.animationDuration = `${6 / (s.speed || 1)}s`; el.style.opacity = String(0.4 + 0.6 * (s.intensity || 1)); }
