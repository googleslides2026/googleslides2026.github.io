import { showModal } from "./modal.js"; import { settings } from "../state/settings.js"; function isMobileDevice() { return /Android|iPhone|iPad|iPod|Mobile/i.test(navigator.userAgent) || window.innerWidth < 640; } export function maybeShowMobileWarning() { if (settings.get().mobileWarningDismissed) return; if (!isMobileDevice()) return; showModal({ title: "NULL isn't meant to run on mobile devices.", body: "Some games, controls, and layouts work much better on desktop. You can still use NULL if you want.", actions: [ { label: "OK", className: "btn-primary", onClick: () => settings.update({ mobileWarningDismissed: true }), }, ], }); }
