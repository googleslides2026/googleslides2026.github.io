export function showModal({ title, body, actions = [], danger = false }) { const backdrop = document.createElement("div"); backdrop.className = "modal-backdrop"; const card = document.createElement("div"); card.className = "modal-card" + (danger ? " danger" : ""); card.setAttribute("role", "dialog"); card.setAttribute("aria-modal", "true"); const h2 = document.createElement("h2"); h2.textContent = title; card.appendChild(h2); if (body) { const p = document.createElement("p"); p.textContent = body; card.appendChild(p); } const actionsRow = document.createElement("div"); actionsRow.className = "modal-actions"; function close() { backdrop.remove(); document.removeEventListener("keydown", onKeydown); } function onKeydown(e) { if (e.key === "Escape") close(); } actions.forEach((a) => { const btn = document.createElement("button"); btn.className = "btn " + (a.className || ""); btn.textContent = a.label; btn.addEventListener("click", () => { a.onClick?.(); if (a.closeOnClick !== false) close(); }); actionsRow.appendChild(btn); }); card.appendChild(actionsRow); backdrop.appendChild(card); backdrop.addEventListener("click", (e) => { if (e.target === backdrop) close(); }); document.addEventListener("keydown", onKeydown); document.body.appendChild(backdrop); const firstBtn = actionsRow.querySelector("button"); firstBtn?.focus(); return { close }; } /** * Danger Zone confirmation. Per spec section 58: BACK is the filled red * button, YES is the outlined red button â€” never reversed. */ export function confirmDanger({ actionText, onConfirm }) { showModal({ title: "Are you sure?", body: actionText, danger: true, actions: [ { label: "Back", className: "btn-danger-filled" }, { label: "Yes", className: "btn-danger-outline", onClick: onConfirm }, ], }); }
