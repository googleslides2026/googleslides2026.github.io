import { settings } from "../state/settings.js"; const STEPS = [ { label: "Welcome", title: "Welcome to NULL", body: "NULL is a clean, fast hub for games and apps â€” built around a simple black-and-white design and a permanent school schedule on the homepage.", }, { label: "Navigation", title: "Getting around", body: "Games, Apps, Announcements, Schedule, Backups, and Settings all live in the top bar. Everything's one click away.", }, { label: "Games", title: "Finding something to play", body: "Search, sort, and filter the library. Star favorites, revisit recent games, or hit Random for a surprise â€” every game opens in a clean fullscreen player.", }, { label: "Schedule", title: "The schedule", body: "The homepage always shows Current Period, Time Left, and Next Period during school hours, plus dedicated states before and after school.", }, { label: "Customization", title: "Make it yours", body: "Switch between dark and light mode, pick an accent color, turn on the animated Glow Border, and customize your browser tab's title and icon.", }, { label: "Game Player", title: "Inside a game", body: "Use Back, Reload, and Fullscreen from the player's top bar. Leaving fullscreen with Esc brings the bar back automatically (configurable in Settings).", }, { label: "Settings", title: "Everything saves automatically", body: "Every setting you change is saved locally right away â€” no save button needed.", }, { label: "Local Data", title: "Your data stays local", body: "Favorites, recently played, and settings are all stored in your browser only. You can back them up or clear them anytime from Settings.", }, { label: "Terms", title: "One last thing", body: "By continuing, you're acknowledging you've reviewed NULL's Terms of Service and related legal pages, linked in the footer.", }, ]; export function maybeShowOnboarding() { if (settings.get().onboardingCompleted) return; let stepIndex = 0; const backdrop = document.createElement("div"); backdrop.className = "modal-backdrop"; const card = document.createElement("div"); card.className = "modal-card onboarding-card"; card.setAttribute("role", "dialog"); card.setAttribute("aria-modal", "true"); const progress = document.createElement("div"); progress.className = "onboarding-progress"; STEPS.forEach(() => { const span = document.createElement("span"); progress.appendChild(span); }); card.appendChild(progress); const content = document.createElement("div"); content.className = "onboarding-content"; card.appendChild(content); const actions = document.createElement("div"); actions.className = "modal-actions"; const nextBtn = document.createElement("button"); nextBtn.className = "btn btn-primary"; actions.appendChild(nextBtn); card.appendChild(actions); function draw() { const step = STEPS[stepIndex]; content.innerHTML = ` <div class="onboarding-step-label">${step.label}</div> <h2 style="margin-bottom:12px">${step.title}</h2> <p class="muted">${step.body}</p> `; [...progress.children].forEach((el, i) => el.classList.toggle("done", i <= stepIndex)); nextBtn.textContent = stepIndex === STEPS.length - 1 ? "Done" : "Next â†’"; } nextBtn.addEventListener("click", () => { if (stepIndex === STEPS.length - 1) { settings.update({ onboardingCompleted: true }); backdrop.remove(); return; } stepIndex++; draw(); }); draw(); backdrop.appendChild(card); document.body.appendChild(backdrop); }
