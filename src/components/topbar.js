import { icon } from "../utilities/icons.js"; import { currentPath } from "../routing/router.js"; import { openSearch } from "./searchOverlay.js"; const NAV_ITEMS = [ { label: "Games", href: "/games" }, { label: "Apps", href: "/apps" }, { label: "Announcements", href: "/announcements" }, { label: "Schedule", href: "/schedule" }, { label: "Backups", href: "/backups" }, { label: "Settings", href: "/settings" }, ]; export function renderTopbar() { const bar = document.createElement("header"); bar.className = "topbar"; const left = document.createElement("div"); left.className = "topbar-left"; const homeBtn = document.createElement("a"); homeBtn.href = "/"; homeBtn.className = "home-btn"; homeBtn.setAttribute("aria-label", "Home"); homeBtn.innerHTML = icon("home"); left.appendChild(homeBtn); const brand = document.createElement("a"); brand.href = "/"; brand.className = "brand"; brand.textContent = "NULL"; left.appendChild(brand); bar.appendChild(left); const right = document.createElement("div"); right.className = "row"; const navLinks = document.createElement("nav"); navLinks.className = "nav-links"; const path = currentPath(); NAV_ITEMS.forEach((item) => { const a = document.createElement("a"); a.href = item.href; a.className = "nav-link" + (path.startsWith(item.href) ? " active" : ""); a.textContent = item.label; navLinks.appendChild(a); }); right.appendChild(navLinks); const searchBtn = document.createElement("button"); searchBtn.className = "nav-icon-btn"; searchBtn.setAttribute("aria-label", "Search"); searchBtn.innerHTML = icon("search"); searchBtn.addEventListener("click", openSearch); right.appendChild(searchBtn); // Mobile menu const mobileBtn = document.createElement("button"); mobileBtn.className = "nav-icon-btn mobile-menu-btn"; mobileBtn.setAttribute("aria-label", "Menu"); mobileBtn.setAttribute("aria-haspopup", "true"); mobileBtn.innerHTML = icon("menu"); right.appendChild(mobileBtn); let mobileMenuEl = null; function closeMobileMenu() { mobileMenuEl?.remove(); mobileMenuEl = null; document.removeEventListener("click", onDocClick, true); } function onDocClick(e) { if (mobileMenuEl && !mobileMenuEl.contains(e.target) && e.target !== mobileBtn) { closeMobileMenu(); } } mobileBtn.addEventListener("click", () => { if (mobileMenuEl) { closeMobileMenu(); return; } mobileMenuEl = document.createElement("div"); mobileMenuEl.className = "mobile-menu"; NAV_ITEMS.forEach((item) => { const a = document.createElement("a"); a.href = item.href; a.textContent = item.label; mobileMenuEl.appendChild(a); }); bar.appendChild(mobileMenuEl); setTimeout(() => document.addEventListener("click", onDocClick, true), 0); }); bar.appendChild(right); return bar; }
