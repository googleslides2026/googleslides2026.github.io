const KEY = "null_recent_v1"; const MAX_ENTRIES = 60; function load() { try { return JSON.parse(localStorage.getItem(KEY) || "[]"); } catch { return []; } } let entries = load(); // [{ id, timestamp }] function persist() { localStorage.setItem(KEY, JSON.stringify(entries)); } export const recent = { record(id) { entries = entries.filter((e) => e.id !== id); entries.unshift({ id, timestamp: Date.now() }); entries = entries.slice(0, MAX_ENTRIES); persist(); }, list: () => entries, clear() { entries = []; persist(); }, };
